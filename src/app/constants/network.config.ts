import { NetworkName, NETWORKS } from './networks';

export const CURRENT_NETWORK: NetworkName = 'localhost'; // <-- Change as needed

export const CURRENT_NETWORK_CONFIG = NETWORKS[CURRENT_NETWORK];
